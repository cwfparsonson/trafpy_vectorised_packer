import setuptools

setuptools.setup(name="trafpy_vectorised_packer",
                 version="1.0",
                 description="Speed-up of the flow packing algorithm proposed in the original TrafPy paper.",
                 packages=setuptools.find_packages(),
                 python_requires='>=3.8')
